# Frontend (React + Vite)

Run locally:

```bash
cd frontend
npm install
npm run dev
```

Build:
```bash
npm run build
```

The app expects Azure Functions endpoints at `/api/*` when deployed via Azure Static Web Apps.
